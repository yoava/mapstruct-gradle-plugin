apply plugin: 'groovy'
apply plugin: 'maven'

group = 'org.mapstruct'
version = '0.9'

repositories {
    jcenter();
}

dependencies {
    compile gradleApi()
    compile localGroovy()
}

// wrapper task
task wrapper(type: Wrapper) {
    gradleVersion = '2.3'
}
