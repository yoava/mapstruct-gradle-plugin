apply plugin: 'groovy'
apply plugin: 'maven'

group = 'org.mapstruct'
version = '1.0'

repositories {
    jcenter();
}

dependencies {
    compile gradleApi()
    compile localGroovy()
}

// wrapper task
task wrapper(type: Wrapper) {
    gradleVersion = '2.3'
}
